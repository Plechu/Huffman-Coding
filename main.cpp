#include "huffman.h"
#include <QApplication>

int main(int argc, char *argv[]){
    // Wygenerowane przez Qt
    QApplication a(argc, argv);
    Huffman w;
    w.show();

    return a.exec();
}
